/*
    functions in set
      .add
      .clear
      .length
      .unique
      .intersection
      .difference
      .remove
      .contains
      .containsall
      .addAll ->takes a list for adding the element in the set
      .toList
      .first
      .last
      .toMap
      .join()
      .followedBy()  ->adds the element after the object element
*/

// import "dart:io";
void main(){
  // var s={1,2,3,4,5};
  // Set <int> t={1,2,6,7,8};
  // t.add(10);
  // print(s.runtimeType);
  // var p=s.difference(t);
  // print(p.runtimeType);
  // var pt=[1,2,3,6,7,8,90,12,2,4,08,5,5555];
  // pt.sort();
  // s.addAll(pt);
  // print(pt.length);
  // // s.forEach((ele) {
  // //   print(ele);
  // // });
  // print(p);
  
  // var ne=p.followedBy(s);
  // print(ne);
  // print(s);
  var t=1.009;
  var c=t.toInt();
  print(c);
}