#include<stdio.h>
int main()
{
    int n
}

/*#include<stdio.h>
int main()
{
    FILE *f,*p;
    // sourcefile the place from where u need to copy the text
    f=fopen("sourcefile.txt","r");
    // newfile (pasted content)
    p=fopen("newfile.txt","w");
    int c;
    while(1)
    {
        c=fgetc(f);
        if(c==EOF)
        break;
        fputc(c,p);
    }
    fclose(f);
    fclose(p);
    return 0;
}*/